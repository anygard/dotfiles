source .bashrc
eval $(/homenfs/nygas/.linuxbrew/bin/brew shellenv)
# be sure to add `/homenfs/nygas/.cargo/bin` to your PATH to be able to run the installed rust binaries
PATH=$PATH:/homenfs/nygas/.cargo/bin
